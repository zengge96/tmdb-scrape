--- name: tmdb-scrape description: 电影信息补全工作流。使用 TMDB API 批量查询电影信息并补全电影元数据（评分、海报、年份、国家、类型等）。 --- # 🎬 TMDB 电影信息补全工作流 本工作流通过 **Node.js 脚本** 进行文件管理，并利用 **Subagents (子智能体)** 并发处理核心的 AI 识别与 API 检索任务，大幅提升批量处理效率。 ## 🛠 工作流概览 ```mermaid graph TD A[原始文件] --> B[Step 1: 生成行号] B --> C[Step 2: 切分批次] C --> D{Step 3: Subagent 并发处理} D -- 子任务 1 --o E[提取/搜索/验证/详情] D -- 子任务 2 --o E D -- 子任务 N --o E E --> F[Step 4: 路径映射] F --> G[Step 5: 最终合并] ``` --- ## 📝 详细操作流程 ### Step 1: 生成带行号的完整文件 为原始数据建立唯一索引。 - **命令**: `node gen_file_with_num.js /tmp/incomplete.txt /tmp/incomplete_with_num.txt` ### Step 2: 获取批次文件 将任务拆分为多个批次（如每批 20-50 条），以便分配给多个 Subagent。 - **命令**: `node get_one_bach_lines.js <输入> <输出> <批次编号> <行数>` ### Step 3: Subagent 并发处理任务 **核心步骤**：针对每一个批次文件，启动一个 Subagent 执行复合任务。多个 Subagent 可同时运行以提升效率。 **告知每个 Subagent 的指令：** 1. **任务目标**：从给定的原始路径中提取电影名，在 TMDB 中搜索并选出最匹配的记录，最后获取完整详情。 2. **输入文件**：`batch-XX.txt`（格式：行号|原始路径）。 3. **处理逻辑**： - **提取**：智能分析路径，剥离组名、编码等杂质，提取"纯净片名"和"年份"。 - **搜索**：调用 `tmdb_search.js` 获取候选列表。 - **验证**：强制要求年份误差 ±1 年，选择最匹配的 TMDB_ID。 - **详情**：调用 `tmdb_details.js` 获取国家、类型、评分、海报等。 4. **输出文件**：`results/batch-XX-details.txt`。 5. **字段规范**：确保输出包含 `行号|标题|TMDB_ID|评分|海报|年份|国家|类型`。 ### Step 4: 路径映射与校验 将 Subagent 产出的详情回填至原始路径，生成各批次的最终结果。 - **命令**: `node map_path.js <原始文件> <batch-XX-details.txt> <错误记录> <成功输出> <错误输出>` - **输出**: `success-XX.txt`, `error-XX.txt` ### Step 5: 合并为最终文件 当所有 Subagent 任务完成后，合并所有结果。 - **命令**: `node merge.js success_ error_ final_success.txt final_error.txt` --- ## 📂 脚本功能矩阵 | 脚本名称 | 核心功能 | 协作对象 | | :--- | :--- | :--- | | `gen_file_with_num.js` | 建立全局索引 | 工作流启动器 | | `tmdb_search.js` | 执行 `search/multi` | Subagent 调用 | | `tmdb_details.js` | 执行 `movie/{id}` 或 `tv/{id}` | Subagent 调用 | | `map_path.js` | 路径与元数据 Join | 工作流管理器 | | `merge.js` | 数据汇总 | 工作流管理器 | --- ## 🚀 快速执行示例 (Subagent 模式) ```bash # 1. 预处理 node gen_file_with_num.js ./movies.txt /tmp/indexed.txt # 2. 准备批次 (假设准备 3 个批次并发) node get_one_bach_lines.js /tmp/indexed.txt /tmp/batch-01.txt 1 50 node get_one_bach_lines.js /tmp/indexed.txt /tmp/batch-02.txt 2 50 node get_one_bach_lines.js /tmp/indexed.txt /tmp/batch-03.txt 3 50 # 3. 启动 Subagents # Subagent 1 处理 batch-01.txt -> batch-01-details.txt # Subagent 2 处理 batch-02.txt -> batch-02-details.txt # Subagent 3 处理 batch-03.txt -> batch-03-details.txt # 4. 映射结果 (每批次完成后执行) node map_path.js ./movies.txt /tmp/batch-01-details.txt /tmp/err1.txt /tmp/success1.txt /tmp/err_final1.txt # 5. 合并 node merge.js success error final_success.txt final_error.txt ``` --- 💡 **提示**: 使用 Subagent 并发时，请监控 TMDB API 的 Rate Limit（频率限制），建议在脚本中加入指数退避重试机制。
